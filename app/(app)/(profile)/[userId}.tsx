import { RamaText } from "@/components/Themed";
import { SafeAreaView } from "react-native-safe-area-context";

export default function ProfileDetailsScreen(){
    return <SafeAreaView>
        <RamaText>Profile Details</RamaText>
    </SafeAreaView>
}