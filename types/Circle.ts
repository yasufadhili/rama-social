export type TCircle ={
    id: string;
    creatorId: string;
    name: string;
    description: string;
    members: string[];
}

export type TContacts = {
    userId: string;
    contacts: string[];
}